using System.Collections.Generic;
using System.Linq;
using System;
using Microsoft.EntityFrameworkCore;

namespace P0_KemoAllen
{
    public class StoreRepositoryLayer
    {
        // int numberOfProducts = Enum.GetNames(typeof(ProductList)).Length; //Gets the number of products in the product list
        // static StoreDbContext DbContext = new StoreDbContext();
        // DbSet<Product> products = DbContext.products; //Database set of products
        // DbSet<Customer> customers = DbContext.customers; //Database set of customers
        // DbSet<Order> orders = DbContext.orders; //Database set of orders
        // DbSet<Location> locations = DbContext.locations; //Database set of locations

        // //Create product, customer, order, location

        
    }
}